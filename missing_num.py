# 2017def miss_(seq):    for i in range(int(len(seq))):        head = int(seq[:i+1])        tail = seq[i+1:]        if tail.startswith(str(head+1)):            tail = seq[len(str(head)):]            print(tail)        # elif        #     print('OK')    returndef miss2_():    sequence = input()    missing  = None    digits = 1    while missing is None and digits <= len(sequence) // 2:        next = int(sequence[:digits])        remaining = sequence[digits:]        while remaining:            next += 1            if remaining.startswith(str(next)):                remaining = remaining[len(str(next)):]            elif missing is None:                missing = next            else:                remaining = ''                missing = None        digits +=1    return# seq=  input('input the num. sequence:')# print(miss_(seq))# miss_(seq)miss2_()